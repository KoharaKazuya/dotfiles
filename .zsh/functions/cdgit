cdgit() {
  if ! builtin command -v peco >/dev/null; then
    printf '[33mThis command depends on [1;3mpeco[0m\n'
    return 1
  fi

  local target="$(cd "$HOME/git" && echo */*/* | xargs -n1 | peco --query "$*")"
  if [ -n "$target" ]; then
    cd "$HOME/git/$target"
  fi
}
