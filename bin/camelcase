#!/usr/bin/env ruby

# 引数が - のみの場合
if ARGV.size() == 1 and ARGV[0] == '-'
  raw_string = STDIN.read()
# 引数が文字列の場合
else
  raw_string = ARGV.join(' ')
end

# 与えられた文字列をキャメルケースに
camel = raw_string
  .split('_')
  .map do |w|
    w[0] = w[0].upcase
    w
  end
  .join()

puts camel
