#!/bin/sh

set -eu

if [ $# = 0 ]; then
  target_file="index.html"
elif [ $# = 1 ]; then
  target_file="$1"
else
  exit 1
fi

if [ "$target_file" != "-" ]; then
  exec >"$target_file"
fi

cat "$(dirname "$0")/mkhtml.template.html"
